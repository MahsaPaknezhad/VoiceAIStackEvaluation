{
  "stt_service_id": "aws_transcribe",
  "stt_service_name": "AWS Transcribe",
  "module": "pipecat.services.aws.stt",
  "class": "AWSTranscribeSTTService",
  "config": {
    "region": "${AWS_REGION}",
    "session_timeout": 60000,
    "enable_partial_results_stabilization": true,
    "partial_results_stability": "high",
    "vocabulary_filter_method": "remove",
    "max_speaker_labels": 1
  },
  "pipeline_timeout": 15.0,
  "cleanup_timeout": 2.0,
  "pipeline_params": {
    "allow_interruptions": true,
    "enable_metrics": true,
    "enable_usage_metrics": true,
    "report_only_initial_ttfb": true
  },
  "instructions": "\n# Configuration for AWS Transcribe\n\nTo use this STT service in bot.py, replace the STT initialization with:\n\nfrom pipecat.services.aws.stt import AWSTranscribeSTTService\n\nstt = AWSTranscribeSTTService(\n    region=\"ap-southeast-2\",\n    session_timeout=60000,\n    enable_partial_results_stabilization=True,\n    partial_results_stability=\"high\"\n)\n"
}